import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    flexGrow: 1,
    padding: 0,
    textAlign: "center",
    margin: "0 auto",
  },
  grid: {
    padding: "2rem",
    alignItems: "center",
  },
  img: {
    width: "100%",
    maxWidth: "300px",
  },
});
