import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
    marginTop: "2rem",
    textAlign: "center",
  },
  btn: {
    marginTop: "1rem",
  },
}));
